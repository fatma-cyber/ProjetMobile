<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartCanteen"
        tools:targetApi="31">

        <!-- ✅ ACTIVITY MANQUANTE AJOUTÉE ICI -->
        <activity
            android:name=".AvisActivity"
            android:exported="false" />

        <activity
            android:name=".GestionReservationsActivity"
            android:exported="false" />

        <!-- Écran de connexion (point d'entrée) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Écran d'inscription -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <!-- Dashboard Étudiant -->
        <activity
            android:name=".EtudiantDashboardActivity"
            android:exported="false" />

        <!-- Mes Réservations -->
        <activity
            android:name=".MesReservationsActivity"
            android:exported="false" />

        <activity
            android:name=".MenuCompletActivity"
            android:exported="false" />

        <!-- Dashboard Personnel -->
        <activity
            android:name=".DashboardPersonnelActivity"
            android:exported="false" />

        <!-- Gestion des menus (Personnel) -->
        <activity
            android:name=".GestionMenuActivity"
            android:exported="false" />
    </application>

</manifest>